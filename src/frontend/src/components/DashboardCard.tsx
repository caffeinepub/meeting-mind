// Legacy BudgetFlow component — not used in Meeting Mind
export default function DashboardCard(_props: {
  totalExpenses: number;
  monthlyIncome: number;
  onQuickAdd: () => void;
}) {
  return null;
}
